# Copie para .env na VPS: /opt/gerador-licencas/.env
# Domínio: https://licencas.inovatitech.com.br
#
# OBRIGATÓRIO: POSTGRES_APP_PASSWORD (senha do banco Docker license-db)

LICENSE_SERVER_HOST=0.0.0.0
LICENSE_SERVER_PORT=8195
TRUST_PROXY=true
PUBLIC_BASE_URL=https://licencas.inovatitech.com.br

SECRET_KEY=GERE-SECRET-FORTE-MIN-64-CHARS
ADMIN_USERNAME=admin
ADMIN_PASSWORD=TROQUE-SENHA-FORTE-ADMIN

# Postgres dedicado (container license-db) — OBRIGATÓRIO
POSTGRES_APP_PASSWORD=TROQUE-SENHA-POSTGRES-FORTE

# Não defina LOCAL_DATABASE_URL com sqlite em produção Docker.
# Com POSTGRES_APP_PASSWORD, o app usa Postgres automaticamente.
# Remova ou comente linhas como: LOCAL_DATABASE_URL=sqlite:///...
#
# Migrations (após deploy):
#   docker compose exec license-server python docker/ensure_migrations.py
# Não use `alembic upgrade head` direto sem POSTGRES_* — pode apontar para SQLite por engano.

# Chave compartilhada com Cloud, Lab e VDE (header X-License-Api-Key)
PRODUCT_API_KEY=GERE-CHAVE-API-PRODUTOS-FORTE

# Stripe (produção)
STRIPE_SECRET_KEY=rk_live_SUA_CHAVE_RESTRITA
STRIPE_WEBHOOK_SECRET=whsec_SEU_WEBHOOK

LICENSE_WEB_PORT=8195
LICENSE_WEB_BIND=0.0.0.0
BLOCK_AFTER_DAYS=30
CANCEL_AFTER_DAYS=45

SYNC_REMOTE_ENABLED=false
